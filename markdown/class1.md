# H1

## H2

### H3

#### H4

##### H5

###### H6

- **粗體**
- _斜體_
- [連結](https://example.com)

```python
print("hello world")
```

# 🐍 Python 第一堂課｜小學生輕鬆懂筆記

---

## ✏️ 一、註解（給人看的說明）

🔹 **註解不會被電腦執行**，只是幫助人看懂程式。

- 單行註解：

```python
# 這是單行註解
```

- 多行註解：

```python
"""
這是多行註解
"""
```

👉 就像在作業旁邊寫小提醒 ✍️

---

## 🔢 二、資料種類（電腦記東西的方法）

| 種類   | 範例           | 說明             |
| ------ | -------------- | ---------------- |
| 整數   | `3`            | 沒有小數點的數字 |
| 浮點數 | `3.14`         | 有小數點的數字   |
| 布林值 | `True / False` | 對或錯           |
| 字串   | `"Hello"`      | 文字             |

```python
n1 = 3
f1 = 3.14
b1 = True
s1 = "Hello"
```

---

## 🖨️ 三、print（把東西顯示出來）

```python
print(3)
print("Hello")
```

👉 `print` 就像「大聲說出來給你看」📣

---

## ➕ 四、數學運算

```python
a = 10
b = 20
```

| 運算 | 寫法     | 意思     |
| ---- | -------- | -------- |
| 加   | `a + b`  | 加法     |
| 減   | `a - b`  | 減法     |
| 乘   | `a * b`  | 乘法     |
| 除   | `a / b`  | 除法     |
| 整除 | `a // b` | 只要整數 |
| 餘數 | `a % b`  | 剩下多少 |
| 次方 | `a ** 2` | a 的平方 |

---

## 🔤 五、字串（文字）玩法

```python
print("hello" + " world")   # 接起來
print("hi" * 3)    # 重複
```

結果：

- `hello world`
- `hihihi`

---

## 🧩 六、f-string（聰明的句子）

```python
name = "Alice"
age = 25
print(f"My name is {name}, and I am {age} years old.")
```

👉 可以把變數放進句子裡，很方便 👍

---

## 📏 七、len（算字有多長）

```python
len("hi")      # 2
len("hello")   # 5
```

👉 看有幾個字（空字串是 0）

---

## 🧠 八、type（看是什麼種類）

```python
type(10)       # int
type(3.14)     # float
type(True)     # bool
type("hi")     # str
```

---

## 🔄 九、資料轉換（換衣服）

```python
int("123")     # 123
float(10)      # 10.0
str(123)       # "123"
bool(0)         # False
bool("hi")      # True
```

👉 把資料換成別的種類 🪄

---

## ⌨️ 十、input（請使用者輸入）

```python
num = int(input("請輸入一個整數: "))
print(num * 10)
```

👉 **input 輸入的東西一開始都是字串！**

---

## ⭕ 十一、算圓形面積

公式：
👉 圓形面積 = π × 半徑 × 半徑

```python
r = int(input("請輸入圓形半徑: "))
pi = 3.14
area = pi * r**2
print(f"半徑為{r}的圓形面積是{area}")
```

---

## 🌐 十二、Streamlit（做網頁畫面）

```python
import streamlit as st
st.title("class1-2")
st.write("Hello")
```

👉 可以做出有標題、文字的網頁 ✨

---

## ✅ 十三、比較與判斷（對或錯）

```python
1 == 1   # True
2 > 1    # True
```

| 符號 | 意思     |
| ---- | -------- |
| `==` | 一樣     |
| `!=` | 不一樣   |
| `>`  | 大於     |
| `<`  | 小於     |
| `>=` | 大於等於 |
| `<=` | 小於等於 |

---

## 🔀 十四、邏輯運算

- not：相反
- and：而且（都要對）
- or：或（有一個對就好）

---

## 🚦 十五、if 判斷（做選擇）

```python
password = input("請輸入密碼: ")

if password == "1234":
    print("歡迎John!")
elif password == "5678":
    print("歡迎Mary!")
else:
    print("密碼錯誤!")
```
